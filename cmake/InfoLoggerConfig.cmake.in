@PACKAGE_INIT@
get_filename_component(InfoLogger_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

set(Common_CMAKE_DIR @Common_DIR@)
find_dependency(Common CONFIG REQUIRED HINTS ${Common_CMAKE_DIR})

if(NOT TARGET AliceO2::InfoLogger)
  include("${InfoLogger_CMAKE_DIR}/InfoLoggerTargets.cmake")
endif()

message(STATUS "InfoLogger ${InfoLogger_VERSION} found")
